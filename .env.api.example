# Example env for API (apps/api)
# Copy to .env (or use spring.config.import) and fill secrets in production

DB_URL=jdbc:postgresql://...
DB_USERNAME=...
DB_PASSWORD=...

# JWT secret must be base64-encoded
JWT_SECRET=...
# Access token expiration in milliseconds (default 1 day)
JWT_EXPIRATION_MS=86400000
